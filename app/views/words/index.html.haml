%h1 Words

%table.table
  %thead
    %tr
      %th Value
      %th Language
      %th Actions
  %tbody
    - @words.each do |word|
      %tr
        %td
          = word.value
        %td
          = word.language
        %td
          = link_to('Show', word_path(word), class: 'btn btn-primary')
